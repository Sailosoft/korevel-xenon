# FeatureRequest
Don't modify this file just do the feature request

## Feature: when opening a chat history
it should open a last ai chat conversation. 

if many conversation currently it open at first conversation. 
it should open to the last conversation from chat history

## Feature: sidebar desktop view
in document shell desktop view still add hambur button to push in or hide the sidebar. 

## Fix: First User Send chat not responding
From initial chat page when i send first user message
The conversation does not respond
I always double send my message for ai conversation respond

there issue with transition and responding the message from initial chat page to conversation page
or has issue witn compiling before it respond so it the first message is staled

## Fix:
On sidebar chat item is always highlighted.

## Feature:
at chat input in conversation view
move the instruction, standard, code button to the buttom of input text alongside with render selection and helper text right side of it or nearest right

## Fix: Fix render type 
the ai instruction does not follow render instruction. 
it adds commentary and question instead of following what render instruction do

## Fix: Fix render component
fix the render component mermaid when it does not render. remove unnecessary package html popup appearing from no where when it can not render mermaid turn that off base on package documentation

## Feature: Agent and Agent Pool of BunnyStudio
use BunnyFeature instead of creating your own component

## Feature: Missing Chat Settings
Missing chat settings on initial chat page

## Fix: Chat Settings z-index issue
Improve chat settings z-index issue


Bunny Case Feature Request
src\modules\bunny-case\src
# 1: Request

In simulator history. Check bunny feature that add row action. add row action that routes to the specific simulator request id

# 2: Request

In simulator component add header options that play all that play each audio but has delay in between. 

# 3: Request

also add  the play audio button at the buttom of chat bubble

# 4: Request

In study: use Bunny CRUD see generate persona. and it will create its record and had a view button to display. use  src\modules\render to render markdown template

Add generating handbook if for case handling or for job interview.

# 5: Check
 case handling or for job interview. check for that application it should be one source of truth object and logic. 

# 6: Request

in trainer it should have trainer history and trainer id similar to simulator id so i could go back on it

# 7: Fix

In trainer module. fix speech to text it was not working
reference bunny studio chat to check how speech to text working

# 8: Fix

In trainer module. When i send (no guide) it still running the validation function it should send no guide right away

# 9: FIx
fix textarea in chat input of trainer module. when long text i  could not expand it. 

# 10: Request
when i click end session it will check the whole session and give me final summary of conversation and guide and rate me for what i am missing of my trainer.

# 11: Fix
Save to playbook not working on trainer module.

# 12: request
Also in study it has generate type of option
if for default, manual, case study, generative instruction, tips and guides, to-do list or beginner instruction or advance instrtuction and etc. 
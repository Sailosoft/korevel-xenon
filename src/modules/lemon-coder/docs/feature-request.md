Project: src\modules\lemon-coder
Instruction:
Do all the requests

# Request - Add scrollbar on chatview
- i am struggling to see the conversation to previous conversation
- add scrollbar i could look previous conversation
- after ai response it should move to the bottom/recent conversation

# Request - plan mode (optional if capable to do skipped if not)
- in plan mode check if file directory it needs to check
- modify the prompt to include which directoty import file that needs to see
- in list it cross check to the directory if there is match
- cross checking does not need direct matching
- e.g "@/src/modules/bla-bla-bla.md" or "../modules/bla-bla-bla.md" then it will check file exist and add to context stash
- The question has capable to mapped out into actual selection
- Where I select on selection that would proceed to send as prompt

# Hotfix - Project Selection
- After i fill up project name and select directory
- after create project it still ask me to select directory

# Request - (If applicable) in creating project also cached the actual file location
- i want to know what the directory still want to cache what specific actual project locate

# Request - Add to file to stash context
- beside save view on content view add button that add file stash context

# Request - Add remove attach context button on attached items
- in top of chat input where attached context list item. there are shorcut button where i could remove stash items there.




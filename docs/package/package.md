## 📦 Korevel‑Xenon – Package Documentation  

Below is a quick‑reference guide for **every** package listed in `package.json`.  
For each entry you’ll find:

| Item | What it is | Typical use‑case | Minimal example |
|------|------------|------------------|-----------------|

Feel free to copy‑paste the snippets into your project and adapt them to your needs.

---  

### Dependencies  

| Package | Purpose | Use‑case | Example |
|---------|---------|----------|---------|
| **[@clerk/nextjs](https://clerk.com/docs/quickstarts/nextjs)** | Authentication SDK that integrates Clerk (email/password, social login, Magic Links, etc.) with Next.js. | Protect routes, manage user sessions, show sign‑in / sign‑up UI. | ```tsx\nimport { ClerkProvider, signedIn, signedOut } from \"@clerk/nextjs\";\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <ClerkProvider>\n      <>{signedIn() ? <Dashboard /> : <SignInPage />}</>\n    </ClerkProvider>\n  );\n}\n``` |
| **[@heroui/react](https://heroui.com/)** | UI component library built on Tailwind CSS (formerly *react‑aria‑components*). | Build accessible, theme‑able UI (buttons, inputs, modals, tables…) quickly. | ```tsx\nimport { Button } from \"@heroui/react\";\n\nexport function Hello() {\n  return <Button color=\"primary\">Click me</Button>;\n}\n``` |
| **[@heroui/styles](https://heroui.com/)** | Global Tailwind‑based CSS reset and design‑token styles for @heroui/react. | Required once per app to enable the Heroui design system. | ```ts\nimport \"@heroui/styles/css.css\"; // entry point (e.g. layout.tsx)\n``` |
| **[@mdxeditor/editor](https://www.mdxeditor.com/)** | Rich‑text editor that works with MDX (markdown + JSX). | Authoring blog posts, docs, or any content that needs live preview + custom components. | ```tsx\nimport { MDXEditor } from \"@mdxeditor/editor\";\nimport \"@mdxeditor/editor/css/theme.css\";\n\nexport function BlogEditor() {\n  return <MDXEditor value=\"# Hello\" onChange={v => console.log(v)} />;\n}\n``` |
| **[@monaco-editor/react](https://github.com/suren-atoyan/monaco-react)** | React wrapper for Monaco Editor (the VS Code code editor). | Embedding a code editor (JSON, YAML, TS, etc.) inside a Next.js page. | ```tsx\nimport { Monaco } from \"@monaco-editor/react\";\n\nexport function CodeEditor() {\n  return (\n    <Monaco\n      height=\"400px\"\n      language=\"typescript\"\n      defaultValue=\"const x = 1;\"\n      onChange={newCode => console.log(newCode)}\n    />\n  );\n}\n``` |
| **awilix** | Lightweight dependency‑injection container for Node/JS/TS. | Decouple services, repositories, utilities; make unit‑testing easier. | ```ts\nimport { createContainer, asClass, asValue } from \"awilix\";\n\nconst container = createContainer();\ncontainer.register({\n  userService: asClass(UserService).singleton(),\n  config: asValue({ apiUrl: \"https://api.example.com\" }),\n});\n\nconst userService = container.resolve(\"userService\");\n``` |
| **class-variance-authority (cva)** | Utility to compose Tailwind class strings with variants. | Building reusable component APIs (e.g., a Button that accepts `size`, `variant`, `color`). | ```ts\nimport { cva } from \"class-variance-authority\";\n\nconst buttonVariants = cva(\n  \"inline-flex items-center justify-center rounded-md text-sm font-medium transition-colors\",\n  {\n    variants: {\n      variant: { primary: \"bg-primary text-primary-foreground\", secondary: \"bg-secondary text-secondary-foreground\" },\n      size: { sm: \"h-9 px-3\", md: \"h-10 px-4\", lg: \"h-11 px-5\" },\n    },\n    defaultVariants: { variant: \"primary\", size: \"md\" },\n  }\n);\n\n// usage:\n<button className={buttonVariants({ variant: \"secondary\", size: \"lg\" })}>OK</button>\n``` |
| **clsx** | Tiny utility for conditionally joining classNames. | Simplify dynamic class strings in JSX. | ```tsx\nimport clsx from \"clsx\";\n\n<div className={clsx(\"p-4\", { \"bg-red-200\": isError, \"border\": hasBorder })}>\n  Content\n</div>\n``` |
| **dexie** | Mini‑wrapper for IndexedDB that offers a simple, promise‑based API. | Client‑side storage for offline‑first apps, caching, or local user data. | ```ts\nimport Dexie from \"dexie\";\n\nconst db = new Dexie(\"MyAppDB\");\ndb.version(1).stores({ notes: \"++id, title, content\" });\n\nexport async function addNote(note) {\n  await db.notes.add(note);\n}\n``` |
| **dexie-react-hooks** | React hooks that bind Dexie queries to component state. | Keep UI in sync with IndexedDB changes automatically. | ```tsx\nimport { useLiveQuery } from \"dexie-react-hooks\";\nimport { db } from \"./db\";\n\nexport function NotesList() {\n  const notes = useLiveQuery(() => db.notes.toArray(), []);\n  return (\n    <ul>\n      {notes.map(n => <li key={n.id}>{n.title}</li>)}\n    </ul>\n  );\n}\n``` |
| **dotenv** | Loads environment variables from a `.env` file into `process.env`. | Keep secrets (API keys, DB URLs) out of source code; works in Node and Next.js (via `next.config.js`). | ```js\n// next.config.js\nrequire(\"dotenv\").config();\n\nmodule.exports = {\n  env: {\n    OPENAI_API_KEY: process.env.OPENAI_API_KEY,\n  },\n};\n``` |
| **handlebars** | Logic‑less templating engine (mustache‑style). | Rendering email templates, server‑side HTML, or any dynamic text output. | ```ts\nimport handlebars from \"handlebars\";\n\nconst source = \"Hello {{name}}, you have {{count}} new messages.\";\nconst template = handlebars.compile(source);\nconst html = template({ name: \"Ada\", count: 5 });\nconsole.log(html); // Hello Ada, you have 5 new messages.\n``` |
| **lucide-react** | Collection of simple, consistent SVG icons (MIT licensed). | Adding icons to UI without pulling in heavy icon packs. | ```tsx\nimport { LucideProps, User } from \"lucide-react\";\n\nexport function Avatar({ name }: { name: string }) {\n  return (\n    <div className=\"flex items-center gap-2\">\n      <User className=\"h-5 w-5\" />\n      <span>{name}</span>\n    </div>\n  );\n}\n``` |
| **marked** | Fast markdown parser & compiler (low‑level). | Convert markdown strings to HTML when you need full control over rendering. | ```ts\nimport marked from \"marked\";\n\nconst html = marked.parse(\"# Hello **world**\");\nconsole.log(html); // <h1>Hello <strong>world</strong></h1>\n``` |
| **mitt** | Tiny (~200b) event emitter. | Simple pub/sub pattern for cross‑component communication (e.g., global toast service). | ```ts\nimport mitt from \"mitt\";\n\ntype Events = {\n  toast: { message: string; type: \"info\" | \"error\" };\n};\nconst emitter = mitt<Events>();\n\n// subscriber\nemitter.on(\"toast\", e => console.log(e.message));\n// publisher\nemitter.emit(\"toast\", { message: \"Saved!\", type: \"info\" });\n``` |
| **next** | The React framework for production‑grade SSR, SSG, ISR, API routes, etc. | Core of the project – pages, routing, data fetching, image optimization. | (Already used via `next dev`, `next build`, …) |
| **openai** | Official Node.js SDK for OpenAI’s REST API (GPT‑4, Whisper, etc.). | Calling LLMs for chat, summarization, code generation, embeddings. | ```ts\nimport OpenAI from \"openai\";\n\nconst openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });\n\nasync function getCompletion(prompt: string) {\n  const resp = await openai.chat.completions.create({\n    model: \"gpt-4o-mini\",\n    messages: [{ role: \"user\", content: prompt }],\n  });\n  return resp.choices[0]?.message.content;\n}\n``` |
| **radix-ui** | Headless, accessible UI primitives (dialog, popover, toggle, etc.). | Build fully accessible custom components while keeping full styling control. | ```tsx\nimport { Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle } from \"@radix-ui/react-dialog\";\n\nexport function DeleteConfirm() {\n  return (\n    <Dialog>\n      <DialogTrigger asChild>\n        <button className=\"btn\">Delete</button>\n      </DialogTrigger>\n      <DialogContent className=\"p-4\">\n        <DialogHeader>\n          <DialogTitle>Delete item?</DialogTitle>\n        </DialogHeader>\n        <p>This action cannot be undone.</p>\n      </DialogContent>\n    </Dialog>\n  );\n}\n``` |
| **react** | Core React library (version 19.2.4). | Building UI components; the foundation of the app. | (Used implicitly) |
| **react-dom** | React renderer for the DOM. | Needed to mount React trees into the browser. | (Used implicitly) |
| **react-markdown** | React component that safely renders markdown to React elements. | Showing user‑generated markdown (blog posts, comments) without dangerouslySetInnerHTML. | ```tsx\nimport ReactMarkdown from \"react-markdown\";\n\nexport function Post({ markdown }: { markdown: string }) {\n  return <ReactMarkdown>{markdown}</ReactMarkdown>;\n}\n``` |
| **shadcn** | Wrapper around the *shadcn/ui* component collection (built on Radix + Tailwind). | Provides ready‑made, accessible UI pieces (alerts, tables, forms) with easy customization. | ```tsx\nimport { Button } from \"shadcn/ui\";\n\nexport function Save() {\n  return <Button variant=\"outline\">Save</Button>;\n}\n``` |
| **tailwind-merge** | Utility to efficiently merge Tailwind class strings and remove duplicates. | Useful when composing classes with `cva`, `clsx`, or conditional logic. | ```tsx\nimport twMerge from \"tailwind-merge\";\nimport { cva } from \"class-variance-authority\";\n\nconst button = cva(\"inline-flex\", { variants: { size: { sm: \"h-8\", lg: \"h-12\" } } });\n\n// usage:\n<button className={twMerge(button({ size: \"lg\" }), \"bg-blue-500 hover:bg-blue-600\")>}\n  Save\n</button>\n``` |
| **tw-animate-css** | Tiny wrapper that lets you use Animate.css classes with Tailwind (e.g., `animate__fadeIn`). | Adding CSS‑based animations without writing keyframes yourself. | ```tsx\nimport \"tw-animate-css\";\n\nexport function FadeInBox() {\n  return <div className=\"animate__fadeIn animate__delay-2s p-4 border\">Hello</div>;\n}\n``` |
| **uuid** | Generate RFC‑compliant UUIDs (v1‑v5). | Creating unique IDs for database records, cache keys, or client‑side objects. | ```ts\nimport { v4 as uuidv4 } from \"uuid\";\n\nconst id = uuidv4(); // e.g., \"9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d\"\n``` |
| **wouter** | Minimalist (~1.2KB) router for React (hook‑based). | Alternative to Next.js routing when you need a client‑only SPA feel or fine‑grained control. | ```tsx\nimport { Route, Switch, useLocation, useNavigate } from \"wouter\";\n\nfunction App() {\n  const navigate = useNavigate();\n  return (\n    <Switch>\n      <Route path=\"/\" component={Home} />\n      <Route path=\"/about\" component={About} />\n      <Route path=\"*\" component={NotFound} />\n    </Switch>\n  );\n}\n``` |
| **yaml** | Parser and stringifier for YAML (supports schema, custom types). | Reading/writing configuration files, front‑matter, or any YAML data. | ```ts\nimport { parse, stringify } from \"yaml\";\n\nconst yamlStr = \"name: Xeon\\nversion: 0.1.0\";\nconst obj = parse(yamlStr); // { name: \"Xeon\", version: \"0.1.0\" }\nconsole.log(stringify(obj));\n``` |
| **zod** | Type‑Script‑first schema validation & static type inference. | Validating form inputs, API payloads, environment variables, etc. | ```ts\nimport { z } from \"zod\";\n\nconst UserSchema = z.object({\n  id: z.number().positive(),\n  email: z.string().email(),\n  role: z.enum([\"admin\", \"user\"]),\n});\n\n// runtime validation\nconst result = UserSchema.safeParse({ id: 1, email: \"a@b.com\", role: \"admin\" });\nif (result.success) console.log(result.data);\n``` |

---  

### DevDependencies  

| Package | Purpose | Use‑case | Example |
|---------|---------|----------|---------|
| **[@tailwindcss/postcss](https://tailwindcss.com/docs/installation#postcss)** | PostCSS plugin that processes Tailwind’s utility‑first CSS. | Enables Tailwind CSS in a Next.js project (via `postcss.config.js`). | ```js\n// postcss.config.js\nmodule.exports = {\n  plugins: {\n    tailwindcss: {},\n    autoprefixer: {},\n  },\n};\n``` |
| **[@types/handlebars](https://www.npmjs.com/package/@types/handlebars)** | TypeScript definitions for the `handlebars` library. | Gives you IDE autocomplete & type safety when using Handlebars in TS. | (No runtime code – just `npm i -D @types/handlebars`.) |
| **[@types/node](https://www.npmjs.com/package/@types/node)** | TS typings for Node.js globals (`process`, `Buffer`, `fs`, …). | Required when writing TS code that touches Node APIs (e.g., custom server, scripts). | (No runtime code.) |
| **[@types/react](https://www.npmjs.com/package/@types/react)** | TS typings for React and React‑DOM. | Enables type‑checking of JSX, component props, hooks, etc. | (No runtime code.) |
| **[@types/react-dom](https://www.npmjs.com/package/@types/react-dom)** | TS typings for React‑DOM specific APIs (`render`, `hydrate`, `createPortal`, …). | Same as above, for DOM‑related React calls. | (No runtime code.) |
| **eslint** | Pluggable JavaScript/TypeScript linter. | Enforces code style, finds potential bugs, and can auto‑fix